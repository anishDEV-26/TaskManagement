@{
    ViewBag.Title = "Task Progress";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/brands.min.css" />

<style>
    .tprog-main-wrapper {
        width: 100%;
    }

    .tprog-page-title {
        font-size: 32px;
        font-weight: bold;
        margin-bottom: 30px;
        color: #1e293b;
    }

    .modal-dialog {
        max-width: 900px;
    }

    .modal-content {
        border-radius: 18px;
        border: none;
    }

    .modal-header {
        padding: 20px 30px;
        border-bottom: 1px solid #e2e8f0;
    }

    .modal-title {
        font-size: 28px;
        font-weight: bold;
    }

    .table-responsive {
        overflow-x: auto;
    }

    .action-btn-wrap {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 5px;
        flex-wrap: nowrap;
        width: 70px;
        margin: 0 auto;
    }

    .action-btn-placeholder {
        width: 30px;
        height: 30px;
        display: inline-block;
    }

    #progressTable th:last-child, #progressTable td:last-child {
        min-width: 90px;
        white-space: nowrap;
    }

    @@media(max-width:900px) {
        .tprog-form-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<div class="tprog-main-wrapper">
    <h1 class="tprog-page-title">
        <i class="fa-solid fa-chart-line"></i> Task Progress
    </h1>
</div>

<div class="toast-container position-fixed top-0 end-0 p-3"></div>

<!-- DELETE CONFIRM MODAL -->
<div class="modal fade" id="deleteProgressModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-sm modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirm Delete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body text-center">
                <i class="fa-solid fa-triangle-exclamation fa-2x text-danger mb-3"></i>
                <p>Are you sure you want to delete this progress?</p>
                <input type="hidden" id="deleteProgressId" />
            </div>
            <div class="modal-footer justify-content-center">
                <button type="button" class="btn btn-danger" onclick="ConfirmDeleteProgress()">Yes</button>
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No</button>
            </div>
        </div>
    </div>
</div>

<div class="table-responsive">
    <table id="progressTable" class="table table-striped" style="width:100%">
        <thead>
            <tr>
                <th class="text-center">Sno</th>
                <th class="text-center">Employee</th>
                <th class="text-center">Task</th>
                <th class="text-center">Progress Status</th>
                <th class="text-center">Update Date</th>
                <th class="text-center">Due Date</th>
                <th class="text-center">Task Progress %</th>
                <th class="text-center" style="width:200px;">Remarks</th>
                <th class="text-center">Action</th>
            </tr>
        </thead>
        <tbody id="progressTableBody"></tbody>
    </table>
</div>

<script src="/javascript/taskprogress.js?v=4"></script>